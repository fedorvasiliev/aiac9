ADDR=:8080
APP_ENV=development

# Требуется для интерактивного режима, если используются модели kimi-*
# (Moonshot AI).
MOONSHOT_API_KEY=
# Опционально: переопределить адрес API (прокси, локальные тесты).
# MOONSHOT_BASE_URL=

# Требуется для интерактивного режима, если используются модели deepseek-*.
DEEPSEEK_API_KEY=
# Опционально: переопределить адрес API (прокси, локальные тесты).
# DEEPSEEK_BASE_URL=
