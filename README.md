# aiac9

Go-приложение-Агент, Сгенеренный при помощи SDD через AI.

## Требования

- Go 1.27+ (если не установлен в этом окружении — поставьте через `brew install go` или https://go.dev/dl/)

## Структура проекта

```
cmd/aiac9/             точка входа (main.go)
internal/cli/          разбор аргументов командной строки, диспетчеризация команд
internal/config/       конфигурация: ./aiac9.config + переменные окружения (последние приоритетнее)
internal/interactive/  консольный wizard-режим (запуск без аргументов)
internal/llm/          общий клиент OpenAI-совместимого chat completions API
internal/kimi/         endpoint провайдера Moonshot AI (Kimi)
internal/deepseek/     endpoint провайдера DeepSeek
internal/promptfile/   парсинг заготовленных промптов из ./prompts
internal/dialogstore/  история диалогов в SQLite (./aiac9.db)
internal/exchangelog/  логирование пар запрос/ответ в ./logs
internal/secretmask/   маскирование значений из переменных окружения
internal/term/         raw-режим терминала (для стрелок в select-шагах)
internal/server/       HTTP-сервер (net/http), маршруты, graceful shutdown
internal/version/      метаданные сборки (версия/коммит/дата), задаются через -ldflags
```

`internal/` закрывает пакеты от импорта извне модуля — это стандартный подход
для приложений, которые не предоставляют публичное Go API.

## Команды

```
aiac9 [-f substr] [-timeout N] запустить интерактивный wizard-режим (LLM)
aiac9 serve [--addr :8080]     запустить HTTP-сервер
aiac9 version                  вывести версию сборки
aiac9 help                     справка
```

### Интерактивный режим

Запуск без аргументов (`aiac9`) включает консольный wizard для общения с LLM
по HTTP API. Провайдер выбирается по имени модели: `deepseek*` — DeepSeek,
всё остальное (включая модель по умолчанию `kimi-k2.6`) — Moonshot AI
(Kimi). Оба используют общий клиент `internal/llm`, отличаясь только
base URL и переменной окружения с ключом.

Все заголовки шагов (label) выводятся жёлтым. На настоящем терминале
select-шаги листаются стрелками вверх/вниз или пробелом (список
закольцован), Enter подтверждает выбор; если stdin не терминал (пайп,
тесты) — выбор по номеру или точному тексту варианта, Enter — принять
вариант по умолчанию.

1. **Шаг D — выбор диалога.** Список берётся из таблицы `dialog` в SQLite
   (см. «Диалоги» ниже), отсортирован по последней активности; всегда
   доступен первый, выбранный по умолчанию пункт `/new` — начать новый
   диалог. Если выбран существующий диалог — в терминал выводятся
   последние 2 пары вопрос/ответ из его истории (в том же стиле, что и
   обычный ответ модели), а вся история этого диалога используется как
   контекст для следующих запросов.
2. **Шаг A — выбор заготовленного промпта.** Список берётся из файлов в
   `./prompts` **во время выполнения** (не при компиляции) плюс первый,
   всегда доступный вариант «Пользовательский промпт» — по умолчанию.
   Формат файла:
   заголовки Markdown (`### Заголовок`), каждый — один из `Model`,
   `System prompt`, `User prompt` (может повторяться — каждое вхождение
   станет отдельным сообщением), `Response format`, `Words limit`, `Stop`,
   `Temperature`; регистр заголовков не важен. Если выбран не «(без
   промпта)», сразу после выбора распарсенные значения выводятся в
   терминал таблицей (первая колонка — жёлтая), пустые строки не
   показываются; если значение любого поля длиннее 80 символов, вместо
   таблицы печатается по блоку `Заголовок:`/значение на каждую секцию
   (заголовок блока — жёлтый). `Model` из файла переопределяет модель по
   умолчанию (`kimi-k2.6`) и, тем самым, провайдера; `Response format` и
   `Stop` уходят в соответствующие поля запроса; `Temperature` — в поле
   `temperature` (нечисловое значение молча игнорируется); `Words limit`
   дописывает фразу «Максимальное количество слов в ответе должно быть
   N.» ко всем `User prompt`.
3. **Шаг T (терминальный) — ввод текста.** Пустое значение допускается.
   Если введён непустой текст, он дописывается ко всем `User prompt` (или,
   если промпт не выбирался, становится единственным user-сообщением).
   Enter отправляет собранный запрос.
4. Запрос логируется в `./logs` (см. ниже) сразу перед отправкой — то есть
   лог-файл появляется ещё до получения ответа. Пока ответ ожидается, в
   терминале крутится анимированный спиннер. Ответ модели печатается
   зелёным; после пустой строки — `prompt_tokens`/`completion_tokens` из
   `usage` этого запроса, `dialog prompt_tokens`/`dialog completion_tokens`
   — те же счётчики, накопленные по всему текущему диалогу (при
   возобновлении существующего диалога на Шаге D — с учётом его истории),
   и время выполнения запроса с точностью до сотых долей секунды (то же
   время дописывается и в лог-файл), и wizard возвращается к шагу A для
   следующего запроса.

Спиннер и цвета показываются только на настоящем терминале — в пайпах и
логах (не-tty stdout) вывод остаётся простым текстом без ANSI-кодов.

Выход в любой момент: `exit`/`quit`, `q` на select-шаге, Ctrl+C или
Ctrl+D (EOF).

Требуется переменная окружения с ключом провайдера, который реально
используется — `MOONSHOT_API_KEY` для Kimi и/или `DEEPSEEK_API_KEY` для
DeepSeek (см. `.env.example`); без неё запрос к соответствующему провайдеру
не отправляется. `MOONSHOT_BASE_URL`/`DEEPSEEK_BASE_URL` — опциональное
переопределение адреса API (например, для прокси или локальных тестов).
Значения, полученные из переменных окружения, при выводе на экран и в
логи маскируются звёздочками (`***`).

Таймауты HTTP-клиента (общие для обоих провайдеров): 30 секунд на
установку соединения, 180 секунд — на весь запрос целиком (включая
ожидание ответа).

Флаги интерактивного режима:
- `-f substr` — показывать на Шаге A только файлы из `./prompts`, в имени
  которых встречается `substr` (например, `aiac9 -f w1d4` покажет только
  `any-w1d4.prompt.md` и подобные).
- `-timeout N` — переопределить таймаут ожидания ответа, в секундах
  (по умолчанию 180).

### Диалоги

Несколько подряд идущих Шагов T образуют один диалог: вся его история
(вопросы, system-сообщения и ответы модели) складывается в один system-
message, который отправляется вместе с каждым новым запросом этого
диалога — сама история не разбивается на отдельные user/assistant
сообщения в запросе. Команда `/clear` или `/new`, введённая на Шаге T,
завершает текущий диалог и возвращает к Шагу D для выбора/создания
следующего; выбор `/new` на самом Шаге D делает то же самое.

Все сообщения диалога (роль + содержимое) сохраняются в SQLite-базу
`./aiac9.db`, таблица `dialog`, под общим `dialog_id`; строка запроса
дополнительно хранит `prompt_tokens`, а строка ответа — `completion_tokens`
этого обмена. Сумма сохранённых `prompt_tokens`/`completion_tokens` всей
истории диалога — стартовое значение счётчиков `dialog prompt_tokens`/
`dialog completion_tokens`, так что они не обнуляются при возврате к
диалогу в новом запуске `aiac9`. Каждый диалог (от выбора на Шаге D до
сброса) обрабатывается в отдельной goroutine — задел на будущее, когда
потребуется вести параллельно десятки/сотни диалогов.

#### История и суммаризация

По умолчанию на Шаге D в системный контекст попадает вся история
выбранного диалога. Если задать `HISTORY_MSG_COUNT` (переменная окружения
или `./aiac9.config`, по умолчанию 20) — активным контекстом остаются
только последние N запрос/ответ пар:
- `HISTORY_SUMMARIZATION=off` (по умолчанию) — более старые пары просто не
  попадают в контекст; в `./aiac9.db` они не трогаются.
- `HISTORY_SUMMARIZATION=on` — каждая более старая пара по очереди
  сжимается отдельным запросом `/chat/completions` последней использованной
  моделью этого диалога (сохраняется вместе с каждым сообщением в
  `dialog`) в краткое summary, которое сохраняется в таблицу
  `dialog_summary` (по одной строке на диалог, обновляется на месте), после
  чего сжатая пара удаляется из `dialog`. Если для диалога есть summary,
  оно добавляется в системный контекст первым, перед последними N живыми
  парами.

Ограничение и суммаризация применяются один раз — при выборе диалога на
Шаге D; в рамках одной непрерывной сессии без возврата к Шагу D история
продолжает расти без сжатия.

#### Context Strategy

`CONTEXT_STRATEGY` (переменная окружения или `./aiac9.config`, регистр
значения не важен) переопределяет поведение из раздела выше, когда задан:

- `Sliding_Window` — вместо summary-логики более старые пары (за пределами
  `HISTORY_MSG_COUNT`) сразу физически удаляются из `dialog`, без обращения
  к LLM и без записи в `dialog_summary`; `HISTORY_SUMMARIZATION`
  игнорируется.
- `STICKY_FACTS` — то же физическое удаление более старых пар, что и в
  `Sliding_Window` (без summary), плюс: к каждому запросу диалога
  добавляется system-инструкция «вычлени факты (ключ-значение) из user
  prompt и добавь их в ответ отдельной секцией `Факты:`»; секция фактов
  вырезается из ответа перед показом/сохранением и сохраняется построчно
  (`ключ: значение`) в таблицу `facts` (по диалогу, обновляется на месте).
  Накопленные факты добавляются в системный контекст каждого следующего
  запроса этого диалога отдельным сообщением и **переживают** удаление
  старых пар — так важное из начала разговора не теряется, даже когда
  сырая история уже стёрта.

Не задан или неизвестное значение — используется обычная логика
`HISTORY_MSG_COUNT`/`HISTORY_SUMMARIZATION`.

Это единственное место в проекте, где сделано исключение из правила
"только стандартная библиотека": для SQLite используется чистый Go-драйвер
[`modernc.org/sqlite`](https://pkg.go.dev/modernc.org/sqlite) (без cgo, не
требует C-тулчейна) — в стандартной библиотеке своего SQL-драйвера нет.

## Быстрый старт

```sh
cp .env.example .env               # заполнить MOONSHOT_API_KEY
cp aiac9.config.example aiac9.config  # опционально: HISTORY_MSG_COUNT и т.п.
go run ./cmd/aiac9                 # интерактивный режим

make run                           # go run ./cmd/aiac9 serve
curl localhost:8080/healthz
```

## Makefile

| Команда      | Действие                              |
|--------------|----------------------------------------|
| `make build` | сборка бинарника в `./bin`             |
| `make run`   | запуск сервера через `go run`          |
| `make test`  | `go test ./...`                        |
| `make vet`   | `go vet ./...`                         |
| `make fmt`   | `gofmt -l -w .`                        |
| `make tidy`  | `go mod tidy`                          |

